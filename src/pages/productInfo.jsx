import React, { useState } from "react";
import { useNavigate } from "react-router-dom";
import "../App.css";

const productInfo = () => {
  return (
    <div className="login-wrapper">
      <span>PRouct info</span>
    </div>
  );
};

export default productInfo;
